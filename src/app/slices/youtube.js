import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const youtube = createSlice({
  name: "youtube",
  initialState,
  reducers: {},
});

// Action creators are generated for each case reducer function
export const {} = youtube.actions;

export default youtube.reducer;
